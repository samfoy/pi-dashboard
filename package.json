{
  "name": "pi-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "./run.sh",
    "dev": "./run.sh",
    "restart": "./restart.sh",
    "restart:build": "./restart.sh --build",
    "build-frontend": "cd frontend && npm install --no-audit --no-fund && npm run build",
    "setup": "npm install --no-audit --no-fund && npm run build-frontend",
    "start:tailscale": "PI_DASH_PORT=7777 node backend/server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "express-ws": "^5.0.2",
    "node-pty": "^1.1.0",
    "uuid": "^10.0.0",
    "ws": "^8.18.0"
  }
}
